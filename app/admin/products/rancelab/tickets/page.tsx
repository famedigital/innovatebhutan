import { TicketCallCentreDesk } from "@/components/admin/ticket-callcentre-desk";

export default function RanceLabTicketsPage() {
  return (
    <TicketCallCentreDesk
      productKey="rancelab"
      title="RanceLab tickets"
      description="Call-centre desk — log complaint, assign, start, acknowledge, resolve + WhatsApp group notify"
    />
  );
}
